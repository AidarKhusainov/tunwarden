# v0.2 safe TUN preview acceptance verification record

This record is the redacted evidence container for issue #31. It is intentionally separate from the checklist so the release-gate result can be reviewed as a concrete record for the exact commit under test.

This file must not be changed to `PASS` unless every required inspection point from `docs/v0.2-acceptance.md` is conclusive on a Tier 1 Linux desktop host. CI-only results, a container, or a host where TUN, route, DNS, nftables, runtime, and process state cannot be inspected are not sufficient.

## Closure rule

- Use `Refs #31` while this record is incomplete or `PENDING`.
- Use `Closes #31` only in the PR that contains a completed, redacted `PASS` record for the exact commit being merged.
- Do not merge a `Closes #31` PR with empty fields, placeholder values, skipped required inspections, or a `PENDING`/`FAIL` decision.

## Verification record

| Field | Value |
| --- | --- |
| Distribution and version | PENDING: run on Ubuntu LTS desktop or Debian stable desktop |
| Kernel version | PENDING |
| Go version | PENDING |
| Commit SHA | PENDING: record the exact PR head SHA under test |
| Xray version/path | PENDING: redacted path/version, no provider config |
| tun2socks version/path | PENDING: redacted path/version |
| Daemon deployment summary | PENDING: record development privileged daemon launcher, user, capabilities, socket path, and environment variables without secrets |
| Profile input type | PENDING: share URI / subscription / existing local profile; do not record secret values |
| `gofmt` result | PENDING |
| `go test ./...` result | PENDING |
| `go run ./cmd/tunwarden version` result | PENDING |
| Initial `doctor` result | PENDING |
| `plan --mode tun` result | PENDING |
| `plan --mode tun --json` result | PENDING |
| Successful `connect --mode tun` result | PENDING |
| Server bypass route check | PENDING |
| Active TUN/route/DNS/firewall/core checks | PENDING |
| `disconnect` cleanup check | PENDING |
| Final `status` result | PENDING |
| Final `doctor` result | PENDING |
| Plain `recover` non-mutating check | PENDING |
| Rollback failure injection | PENDING |
| Rollback final-state cleanup check | PENDING |
| Explicit recovery dependency | #29 completed via #64; still confirm `recover`, `recover --execute --yes`, and `recover --execute --yes --json` on the acceptance host |
| Known limitations recorded | PENDING |

## Redacted evidence summary

Record summaries only. Keep raw transcripts and snapshots outside the repository unless they have been reviewed and redacted.

### Automated checks

```text
gofmt: PENDING
go test ./...: PENDING
go run ./cmd/tunwarden version: PENDING
go run ./cmd/tunwarden doctor: PENDING
go run ./cmd/tunwarden recover: PENDING
```

### Host inspection coverage

```text
TUN link inspection: PENDING
Route table inspection: PENDING
Policy rule inspection: PENDING
systemd-resolved DNS inspection: PENDING
nftables inspection: PENDING
firewall frontend inspection: PENDING
Xray process inspection: PENDING
tun2socks process inspection: PENDING
/run/tunwarden inspection: PENDING
```

### Success-path result

```text
connect --mode tun: PENDING
active status: PENDING
server route bypass: PENDING
disconnect: PENDING
post-disconnect clean state: PENDING
```

### Rollback-path result

```text
failure injected: PENDING
rollback completed: PENDING
post-rollback clean state: PENDING
plain recover remained non-mutating: PENDING
```

### Explicit recovery dependency result

```text
issue/PR dependency: #29 / #64
recover dry-run on acceptance host: PENDING
recover --execute --yes on acceptance host: PENDING
recover --execute --yes --json on acceptance host: PENDING
```

## Final release-gate decision

```text
v0.2.0 safe TUN preview acceptance: PENDING
```

A `PASS` decision requires every required inspection point to be conclusive. Missing permissions, missing host tools, unredacted secrets in logs, stale TunWarden-owned state after disconnect, failed rollback, or an incomplete explicit recovery dependency are release blockers.
