# Polkit

Polkit is optional package metadata, not a primary user workflow.

Keep permanent authorization and socket-boundary rules in [State and security](./state-and-security.md) and package layout details in [Debian package](./debian-package.md).
