#!/bin/sh
set -eu

if [ "${1:-}" = "remove" ] || [ "${1:-}" = "deconfigure" ]; then
	if command -v deb-systemd-invoke >/dev/null 2>&1; then
		deb-systemd-invoke stop podlazd.service >/dev/null || true
	fi
fi

exit 0
